[pytest]
markers =
    case_id
addopts = --gherkin-terminal-reporter -vv --alluredir=allure_results --reruns 2
testpaths = tests
log_file = pytest.log
log_file_level = 1
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
;log_cli=true